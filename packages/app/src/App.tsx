import React, { ReactElement } from 'react';
import {Button, Input} from "@aqua/components";

const App = (): ReactElement => {
  return (
    <div className="App">
      <Button />
      <Input />
    </div>
  );
};

export default App;
